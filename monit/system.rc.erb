check system <%= fqdn %>
    if loadavg (15min) > 25 for 5 cycles then alert 
    if memory usage > 90% then alert
    if swap usage > 50% then alert
    if cpu usage (user) > 80% then alert
    if cpu usage (system) > 50% then alert
    if cpu usage (wait) > 20% then alert
    
check filesystem rootfs with path /
    if space usage > 90 % then alert
    if inode usage > 90 % then alert
       
## Check for ERROR TRACE CRITICAL errors
check file dmesg with path /var/log/dmesg
    if match '(ERROR|Error|CRITICAL|Critical)' then alert

#check file syslog with path /var/log/syslog
#    if match '(ERROR|Error|CRITICAL|Critical)' then alert
    
#check kern  log
check file kern-log with path /var/log/kern.log
    if match '(ERROR|Error|CRITICAL|Critical)' then alert

check program HDD_80 with path "/opt/monitplugins/disk/diskcheck.sh"
    if status > 0  then  alert
    
